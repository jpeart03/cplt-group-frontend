const SignedInAs = () => {
  let username = "MrPlaceholder"
  return (
    <div style={{display: 'flex',  justifyContent:'center'}}>
      <h3>Signed in as {username}</h3>
    </div>
  )
}

export default SignedInAs